# Gradle cache and daemon state
.gradle/

# Re-include the wrapper JAR excluded by the root *.jar rule
!gradle/wrapper/gradle-wrapper.jar

# Kotlin incremental compilation cache
.kotlin/

# Spring Boot DevTools restart trigger
.reloadtrigger

# devtool start-proxy/stop-proxy bookkeeping (see tui/cmd/devtool)
devtool.pid
devtool.log
