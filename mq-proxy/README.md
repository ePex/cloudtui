# mq-proxy

Spring Boot REST proxy for Amazon MQ (ActiveMQ) queue operations over
JMS/OpenWire. The `local` Spring profile starts an embedded ActiveMQ broker
in the same JVM — this is the Docker-free local dev stack.

Not yet scaffolded — see root `CLAUDE.md` and `api/` for the contract.
