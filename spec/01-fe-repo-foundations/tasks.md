# Tasks — Repo foundations

Plan: [plan.md](plan.md)

1. [x] Create top-level directory layout (`tui/`, `spec/`).
2. [x] Initialise Go module in `tui/` with standard `cmd/tui/` entry point and `internal/` package structure.
3. [x] Verify `go build ./...` succeeds from `tui/`.
